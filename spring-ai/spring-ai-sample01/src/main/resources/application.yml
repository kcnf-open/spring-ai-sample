server:
    port: 8082
    context-path: /

spring:
    ai:
        openai:
            api-key: ${DEEPSEEK_API_KEY}
            base-url: https://api.openai-proxy.org
            chat:
                options:
                    model: gpt-3.5-turbo